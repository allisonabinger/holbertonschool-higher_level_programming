/**
 * Updates the text of the <header> element when DIV#update_header is clicked
 * Uses jQuery API, based on 6-main.html
 */
$('#update_header').click(function () {
  $('header').text('New Header!!!');
});
