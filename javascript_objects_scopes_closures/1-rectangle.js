#!/usr/bin/node
/**
 * contains a class Rectangle
 * takes two arguments w and h
 * initializes instance att width with the value of w
 * initializes instance att height with the value of h
 */
class Rectangle {
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
}
module.exports = Rectangle;
